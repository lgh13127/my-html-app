# Enable AndroidX support
android.useAndroidX=true
# Enable Jetifier (converts old support libs to AndroidX if needed)
android.enableJetifier=true

# Optional but recommended JVM args for CI builds
org.gradle.jvmargs=-Xmx1536m