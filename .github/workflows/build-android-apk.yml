name: Build Android APK with Agora Audio
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GRADLE_HOME: /usr/local/gradle-5.5.1
      JAVA_HOME: /usr/lib/jvm/temurin-8-jdk-amd64
      ANDROID_HOME: /usr/local/android-sdk
      ANDROID_SDK_ROOT: /usr/local/android-sdk
      PATH: /usr/local/gradle-5.5.1/bin:/usr/local/bin:/usr/bin:/usr/local/android-sdk/tools:/usr/local/android-sdk/platform-tools:/usr/local/android-sdk/build-tools/29.0.3:/usr/lib/jvm/temurin-8-jdk-amd64/bin

    steps:
      - uses: actions/checkout@v4

      # 1. 安装Gradle 5.5.1
      - name: Install Gradle 5.5.1
        run: |
          sudo apt-get remove -y gradle
          sudo rm -rf /usr/share/gradle*
          wget -q https://services.gradle.org/distributions/gradle-5.5.1-bin.zip -O /tmp/gradle-5.5.1.zip
          sudo unzip -q /tmp/gradle-5.5.1.zip -d /usr/local/
          sudo ln -sf /usr/local/gradle-5.5.1/bin/gradle /usr/bin/gradle
          gradle -v

      # 2. 安装Node.js 14
      - name: Install Node.js 14
        uses: actions/setup-node@v4
        with:
          node-version: 14.x

      # 3. 安装Cordova 9.0.0
      - name: Install Cordova
        run: |
          npm install -g cordova@9.0.0
          cordova -v

      # 4. 安装Android SDK 29
      - name: Install Android SDK 29
        run: |
          sudo mkdir -p $ANDROID_HOME && sudo chmod -R 777 $ANDROID_HOME
          curl -fsSL -o $ANDROID_HOME/sdk-tools.zip https://dl.google.com/android/repository/sdk-tools-linux-4333796.zip
          unzip -o $ANDROID_HOME/sdk-tools.zip -d $ANDROID_HOME > /dev/null 2>&1
          yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses > /dev/null 2>&1
          $ANDROID_HOME/tools/bin/sdkmanager "platforms;android-29" "build-tools;29.0.3" "platform-tools" "extras;android;m2repository" "extras;google;m2repository" > /dev/null 2>&1

      # 5. 配置Java 8
      - name: Setup Java 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'

      # 6. 创建Cordova项目 + 安装插件 + 配置Agora
      - name: Create Cordova Project with Agora
        run: |
          # 创建项目
          cordova create my-app com.example.agoraaudio AgoraAudioCall
          cd my-app
          
          # 添加Android平台
          cordova platform add android@8.1.0 --save
          
          # 添加必要的插件
          cordova plugin add cordova-plugin-whitelist
          cordova plugin add cordova-plugin-camera
          cordova plugin add cordova-plugin-media-capture
          cordova plugin add cordova-plugin-file
          cordova plugin add cordova-plugin-device
          cordova plugin add cordova-plugin-inappbrowser
          cordova plugin add cordova-plugin-screen-orientation
          
          # 添加支持WebView中WebRTC的插件
          cordova plugin add cordova-plugin-android-permissions
          cordova plugin add cordova-plugin-webrtc
          
          # 创建完整的项目结构
          mkdir -p www/css www/js www/img
          
          # 创建图标和启动画面文件（使用简单的base64编码图片）
          # 创建图标文件
          cat > create_icon.sh << 'EOF'
          #!/bin/bash
          # 创建一个简单的图标PNG文件
          
          # 安装必要的工具
          sudo apt-get update
          sudo apt-get install -y imagemagick
          
          # 创建48x48图标
          convert -size 48x48 xc:#667eea \
            -fill "#764ba2" -draw "circle 24,24 12,12" \
            -fill "white" -pointsize 24 -gravity center -draw "text 0,0 'A'" \
            www/img/icon.png
          
          # 创建各种尺寸的图标
          convert www/img/icon.png -resize 36x36 www/img/icon-ldpi.png
          convert www/img/icon.png -resize 48x48 www/img/icon-mdpi.png
          convert www/img/icon.png -resize 72x72 www/img/icon-hdpi.png
          convert www/img/icon.png -resize 96x96 www/img/icon-xhdpi.png
          convert www/img/icon.png -resize 144x144 www/img/icon-xxhdpi.png
          convert www/img/icon.png -resize 192x192 www/img/icon-xxxhdpi.png
          
          # 创建启动画面
          convert -size 800x1280 gradient:#667eea-#764ba2 \
            -fill "white" -pointsize 60 -gravity center -draw "text 0,-100 '声网音频'" \
            -pointsize 40 -gravity center -draw "text 0,50 '通话应用'" \
            www/img/splash.png
          
          echo "图标和启动画面创建完成"
          EOF
          
          chmod +x create_icon.sh
          ./create_icon.sh
          
          # 复制主HTML文件（声网音频通话界面）
          cat > www/index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="zh-CN">
          <head>
              <meta charset="UTF-8">
              <meta http-equiv="Content-Security-Policy" content="default-src 'self' https://*.agora.io https://download.agora.io https://cdnjs.cloudflare.com 'unsafe-inline' 'unsafe-eval'; media-src * data: blob:;">
              <title>声网音频通话</title>
              <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
              <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
              <style>
                  /* 所有CSS样式与提供的index.html完全一致 */
                  * {
                      margin: 0;
                      padding: 0;
                      box-sizing: border-box;
                      font-family: 'Segoe UI', 'Microsoft YaHei', sans-serif;
                  }
                  
                  body {
                      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                      min-height: 100vh;
                      padding: 20px;
                      overflow-x: hidden;
                  }
                  
                  .container {
                      max-width: 1000px;
                      margin: 0 auto;
                      background: white;
                      border-radius: 20px;
                      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
                      overflow: hidden;
                      padding: 30px;
                  }
                  
                  .header {
                      text-align: center;
                      margin-bottom: 30px;
                      padding-bottom: 20px;
                      border-bottom: 2px solid #f0f0f0;
                  }
                  
                  .header h1 {
                      color: #333;
                      font-size: 28px;
                      margin-bottom: 10px;
                  }
                  
                  .header p {
                      color: #666;
                      font-size: 16px;
                  }
                  
                  .control-panel {
                      background: #f8f9fa;
                      border-radius: 15px;
                      padding: 25px;
                      margin-bottom: 30px;
                      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
                  }
                  
                  .input-group {
                      display: flex;
                      gap: 15px;
                      margin-bottom: 20px;
                      flex-wrap: wrap;
                  }
                  
                  .input-field {
                      flex: 1;
                      min-width: 200px;
                  }
                  
                  .input-field label {
                      display: block;
                      margin-bottom: 8px;
                      font-weight: 600;
                      color: #555;
                      font-size: 14px;
                  }
                  
                  .input-field input {
                      width: 100%;
                      padding: 15px;
                      border: 2px solid #e0e0e0;
                      border-radius: 10px;
                      font-size: 16px;
                      transition: all 0.3s;
                  }
                  
                  .input-field input:focus {
                      outline: none;
                      border-color: #667eea;
                      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
                  }
                  
                  .input-field input::placeholder {
                      color: #aaa;
                  }
                  
                  .token-hint {
                      font-size: 12px;
                      color: #888;
                      margin-top: 5px;
                      display: flex;
                      align-items: center;
                      gap: 5px;
                  }
                  
                  .token-hint i {
                      color: #ff9800;
                  }
                  
                  .button-group {
                      display: flex;
                      gap: 15px;
                      margin-bottom: 20px;
                      flex-wrap: wrap;
                  }
                  
                  button {
                      padding: 15px 30px;
                      border: none;
                      border-radius: 10px;
                      font-size: 16px;
                      font-weight: 600;
                      cursor: pointer;
                      transition: all 0.3s;
                      display: flex;
                      align-items: center;
                      justify-content: center;
                      gap: 8px;
                      min-width: 140px;
                  }
                  
                  button:hover {
                      transform: translateY(-2px);
                      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
                  }
                  
                  button:disabled {
                      opacity: 0.6;
                      cursor: not-allowed;
                      transform: none !important;
                      box-shadow: none !important;
                  }
                  
                  .call-btn {
                      background: linear-gradient(to right, #667eea, #764ba2);
                      color: white;
                  }
                  
                  .call-btn:hover {
                      background: linear-gradient(to right, #5a6fd8, #6b4190);
                  }
                  
                  .call-btn.in-call {
                      background: linear-gradient(to right, #ff6b6b, #ff5252);
                  }
                  
                  .call-btn.in-call:hover {
                      background: linear-gradient(to right, #ff5252, #e53935);
                  }
                  
                  .mic-btn {
                      background: #4CAF50;
                      color: white;
                  }
                  
                  .mic-btn:hover {
                      background: #43a047;
                  }
                  
                  .mic-btn.muted {
                      background: #ff6b6b;
                  }
                  
                  .mic-btn.muted:hover {
                      background: #ff5252;
                  }
                  
                  .members-container {
                      background: white;
                      border-radius: 15px;
                      padding: 25px;
                      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
                  }
                  
                  .members-header {
                      display: flex;
                      justify-content: space-between;
                      align-items: center;
                      margin-bottom: 20px;
                      padding-bottom: 15px;
                      border-bottom: 2px solid #f0f0f0;
                  }
                  
                  .members-header h2 {
                      color: #333;
                      font-size: 22px;
                  }
                  
                  .members-count {
                      background: #667eea;
                      color: white;
                      padding: 5px 15px;
                      border-radius: 20px;
                      font-weight: 600;
                  }
                  
                  .members-list {
                      display: grid;
                      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
                      gap: 20px;
                  }
                  
                  .member-card {
                      background: #f8f9fa;
                      border-radius: 12px;
                      padding: 20px;
                      display: flex;
                      align-items: center;
                      gap: 15px;
                      transition: all 0.3s;
                      border: 2px solid transparent;
                  }
                  
                  .member-card:hover {
                      transform: translateY(-3px);
                      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
                      border-color: #667eea;
                  }
                  
                  .member-card.local {
                      background: linear-gradient(135deg, #e3f2fd, #f3e5f5);
                  }
                  
                  .avatar {
                      width: 60px;
                      height: 60px;
                      border-radius: 50%;
                      display: flex;
                      align-items: center;
                      justify-content: center;
                      font-size: 24px;
                      font-weight: bold;
                      color: white;
                      flex-shrink: 0;
                  }
                  
                  .avatar.local {
                      background: linear-gradient(135deg, #667eea, #764ba2);
                  }
                  
                  .avatar.remote {
                      background: linear-gradient(135deg, #4CAF50, #2E7D32);
                  }
                  
                  .member-info {
                      flex: 1;
                  }
                  
                  .member-name {
                      font-weight: 600;
                      color: #333;
                      font-size: 18px;
                      margin-bottom: 5px;
                  }
                  
                  .member-uid {
                      color: #666;
                      font-size: 14px;
                      margin-bottom: 8px;
                  }
                  
                  .member-status {
                      display: inline-flex;
                      align-items: center;
                      gap: 5px;
                      padding: 3px 10px;
                      border-radius: 12px;
                      font-size: 12px;
                      font-weight: 600;
                  }
                  
                  .status-online {
                      background: #e8f5e9;
                      color: #2E7D32;
                  }
                  
                  .status-muted {
                      background: #ffebee;
                      color: #c62828;
                  }
                  
                  .status-speaker {
                      background: #e3f2fd;
                      color: #1565c0;
                  }
                  
                  .member-actions {
                      display: flex;
                      gap: 8px;
                  }
                  
                  .action-btn {
                      width: 36px;
                      height: 36px;
                      border-radius: 50%;
                      border: none;
                      cursor: pointer;
                      display: flex;
                      align-items: center;
                      justify-content: center;
                      transition: all 0.3s;
                      font-size: 18px;
                  }
                  
                  .mute-btn {
                      background: #ffebee;
                      color: #c62828;
                  }
                  
                  .mute-btn:hover {
                      background: #ffcdd2;
                  }
                  
                  .mute-btn.muted {
                      background: #e0e0e0;
                      color: #616161;
                  }
                  
                  .empty-state {
                      text-align: center;
                      padding: 40px 20px;
                      color: #666;
                  }
                  
                  .empty-state i {
                      font-size: 48px;
                      margin-bottom: 20px;
                      display: block;
                  }
                  
                  .footer {
                      text-align: center;
                      margin-top: 30px;
                      color: #666;
                      font-size: 14px;
                      padding-top: 20px;
                      border-top: 1px solid #eee;
                  }
                  
                  .call-status {
                      text-align: center;
                      margin: 20px 0;
                      padding: 15px;
                      border-radius: 10px;
                      background: #f8f9fa;
                      font-weight: 600;
                      display: none;
                  }
                  
                  .call-status.connected {
                      background: #d4edda;
                      color: #155724;
                      border: 1px solid #c3e6cb;
                  }
                  
                  .call-status.disconnected {
                      background: #f8d7da;
                      color: #721c24;
                      border: 1px solid #f5c6cb;
                  }
                  
                  /* 音频设备选择器样式 */
                  .device-selector {
                      margin-top: 20px;
                      padding: 20px;
                      background: #f0f7ff;
                      border-radius: 12px;
                      border-left: 4px solid #667eea;
                  }
                  
                  .device-selector h3 {
                      margin-bottom: 15px;
                      color: #333;
                      font-size: 18px;
                      display: flex;
                      align-items: center;
                      gap: 10px;
                  }
                  
                  .device-selector h3 i {
                      color: #667eea;
                  }
                  
                  .device-row {
                      display: flex;
                      flex-wrap: wrap;
                      gap: 15px;
                      margin-bottom: 15px;
                  }
                  
                  .device-group {
                      flex: 1;
                      min-width: 200px;
                  }
                  
                  .device-group label {
                      display: block;
                      margin-bottom: 8px;
                      font-weight: 600;
                      color: #555;
                      font-size: 14px;
                  }
                  
                  .device-select {
                      width: 100%;
                      padding: 12px 15px;
                      border: 2px solid #e0e0e0;
                      border-radius: 8px;
                      font-size: 14px;
                      background: white;
                      color: #333;
                      transition: all 0.3s;
                      cursor: pointer;
                  }
                  
                  .device-select:focus {
                      outline: none;
                      border-color: #667eea;
                      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
                  }
                  
                  .device-select:disabled {
                      background: #f5f5f5;
                      color: #999;
                      cursor: not-allowed;
                  }
                  
                  .device-status {
                      margin-top: 8px;
                      font-size: 12px;
                      color: #666;
                      display: flex;
                      align-items: center;
                      gap: 5px;
                  }
                  
                  .device-status.active {
                      color: #2E7D32;
                  }
                  
                  .device-status.inactive {
                      color: #c62828;
                  }
                  
                  .device-actions {
                      display: flex;
                      gap: 10px;
                      margin-top: 10px;
                  }
                  
                  .refresh-btn {
                      padding: 10px 15px;
                      background: #6c757d;
                      color: white;
                      border: none;
                      border-radius: 8px;
                      font-size: 14px;
                      cursor: pointer;
                      transition: all 0.3s;
                      display: flex;
                      align-items: center;
                      gap: 8px;
                  }
                  
                  .refresh-btn:hover {
                      background: #5a6268;
                      transform: translateY(-1px);
                  }
                  
                  .refresh-btn:disabled {
                      opacity: 0.6;
                      cursor: not-allowed;
                      transform: none !important;
                  }
                  
                  .test-btn {
                      padding: 10px 15px;
                      background: #17a2b8;
                      color: white;
                      border: none;
                      border-radius: 8px;
                      font-size: 14px;
                      cursor: pointer;
                      transition: all 0.3s;
                      display: flex;
                      align-items: center;
                      gap: 8px;
                  }
                  
                  .test-btn:hover {
                      background: #138496;
                      transform: translateY(-1px);
                  }
                  
                  .test-btn:disabled {
                      opacity: 0.6;
                      cursor: not-allowed;
                      transform: none !important;
                  }
                  
                  .volume-meter {
                      margin-top: 10px;
                      height: 8px;
                      background: #e0e0e0;
                      border-radius: 4px;
                      overflow: hidden;
                      position: relative;
                      display: none;
                  }
                  
                  .volume-level {
                      height: 100%;
                      background: linear-gradient(to right, #4CAF50, #8BC34A, #FFEB3B, #FF9800, #F44336);
                      border-radius: 4px;
                      transition: width 0.1s ease;
                  }
                  
                  /* 媒体源选项样式 */
                  .source-group {
                      margin-bottom: 15px;
                  }
                  
                  .source-group label {
                      display: block;
                      margin-bottom: 8px;
                      font-weight: 600;
                      color: #555;
                      font-size: 14px;
                      display: flex;
                      align-items: center;
                      gap: 8px;
                  }
                  
                  .source-options {
                      display: flex;
                      gap: 10px;
                      flex-wrap: wrap;
                  }
                  
                  .source-option {
                      display: flex;
                      align-items: center;
                      gap: 5px;
                      cursor: pointer;
                      padding: 8px 12px;
                      border-radius: 6px;
                      background: #f0f0f0;
                      transition: all 0.3s;
                  }
                  
                  .source-option:hover {
                      background: #e0e0e0;
                  }
                  
                  .source-option.active {
                      background: #e3f2fd;
                      border: 1px solid #667eea;
                  }
                  
                  .source-option input[type="radio"] {
                      margin: 0;
                  }
                  
                  .source-option label {
                      margin: 0;
                      font-weight: normal;
                      cursor: pointer;
                  }
                  
                  /* 音频处理选项样式 */
                  .processing-group {
                      margin-top: 15px;
                  }
                  
                  .processing-group label {
                      display: block;
                      margin-bottom: 8px;
                      font-weight: 600;
                      color: #555;
                      font-size: 14px;
                      display: flex;
                      align-items: center;
                      gap: 8px;
                  }
                  
                  .processing-options {
                      display: flex;
                      gap: 15px;
                      flex-wrap: wrap;
                  }
                  
                  .processing-option {
                      display: flex;
                      align-items: center;
                      gap: 5px;
                      cursor: pointer;
                      padding: 8px 12px;
                      border-radius: 6px;
                      background: #f0f0f0;
                      transition: all 0.3s;
                  }
                  
                  .processing-option:hover {
                      background: #e0e0e0;
                  }
                  
                  .processing-option.active {
                      background: #e8f5e9;
                      border: 1px solid #4CAF50;
                  }
                  
                  .processing-option input[type="checkbox"] {
                      margin: 0;
                  }
                  
                  .processing-option label {
                      margin: 0;
                      font-weight: normal;
                      cursor: pointer;
                  }
                  
                  .tooltip {
                      font-size: 12px;
                      color: #666;
                      margin-left: 4px;
                      cursor: help;
                  }
                  
                  @media (max-width: 768px) {
                      .container {
                          padding: 20px;
                      }
                      
                      .input-group {
                          flex-direction: column;
                      }
                      
                      .input-field {
                          width: 100%;
                      }
                      
                      .button-group {
                          justify-content: center;
                      }
                      
                      button {
                          flex: 1;
                          min-width: 120px;
                      }
                      
                      .members-list {
                          grid-template-columns: 1fr;
                      }
                      
                      .device-row {
                          flex-direction: column;
                      }
                      
                      .device-group {
                          min-width: 100%;
                      }
                      
                      .source-options {
                          flex-direction: column;
                      }
                      
                      .processing-options {
                          flex-direction: column;
                      }
                  }
                  
                  @media (max-width: 480px) {
                      .button-group {
                          flex-direction: column;
                      }
                      
                      button {
                          width: 100%;
                      }
                      
                      .device-actions {
                          flex-direction: column;
                      }
                  }
              </style>
          </head>
          <body>
              <div class="container">
                  <!-- 头部 -->
                  <div class="header">
                      <h1>🎤 声网音频通话</h1>
                      <p>随时随地，清晰通话 | 支持多人语音会议</p>
                  </div>
                  
                  <!-- 控制面板 -->
                  <div class="control-panel">
                      <div class="input-group">
                          <div class="input-field">
                              <label for="channelName"><i class="fas fa-hashtag"></i> 频道名称</label>
                              <input type="text" id="channelName" placeholder="请输入频道名称，如：meeting-room-1" value="test-channel-123">
                          </div>
                          
                          <div class="input-field">
                              <label for="tokenInput"><i class="fas fa-key"></i> 鉴权令牌 (Token)</label>
                              <input type="text" id="tokenInput" placeholder="请输入鉴权令牌 (可选)">
                              <div class="token-hint">
                                  <i class="fas fa-info-circle"></i>
                                  <span>在安全模式下需要Token，测试时可留空使用临时Token</span>
                              </div>
                          </div>
                      </div>
                      
                      <div class="button-group">
                          <button class="call-btn" id="callToggle">
                              <i class="fas fa-phone-alt"></i>
                              <span id="callText">加入通话</span>
                          </button>
                          <button class="mic-btn" id="micToggle" disabled>
                              <i class="fas fa-microphone"></i>
                              <span id="micText">开启话筒</span>
                          </button>
                      </div>
                      
                      <!-- 音频设备选择器 -->
                      <div class="device-selector">
                          <h3><i class="fas fa-sliders-h"></i> 音频设备设置</h3>
                          
                          <!-- 媒体源选择 -->
                          <div class="source-group">
                              <label><i class="fas fa-wave-square"></i> 音频来源</label>
                              <div class="source-options">
                                  <div class="source-option active" id="micSourceOption">
                                      <input type="radio" id="micSource" name="audioSource" value="microphone" checked>
                                      <label for="micSource">麦克风</label>
                                  </div>
                              </div>
                          </div>
                          
                          <div class="device-row">
                              <div class="device-group">
                                  <label for="micSelect"><i class="fas fa-microphone"></i> 麦克风（输入设备）</label>
                                  <select id="micSelect" class="device-select" disabled>
                                      <option value="">正在获取设备列表...</option>
                                  </select>
                                  <div id="micStatus" class="device-status inactive">
                                      <i class="fas fa-circle"></i>
                                      <span>未选择麦克风</span>
                                  </div>
                              </div>
                              
                              <div class="device-group">
                                  <label for="speakerSelect"><i class="fas fa-volume-up"></i> 扬声器（输出设备）</label>
                                  <select id="speakerSelect" class="device-select" disabled>
                                      <option value="">正在获取设备列表...</option>
                                  </select>
                                  <div id="speakerStatus" class="device-status inactive">
                                      <i class="fas fa-circle"></i>
                                      <span>使用默认扬声器</span>
                                  </div>
                              </div>
                          </div>
                          
                          <!-- 音频处理选项 -->
                          <div class="processing-group">
                              <label><i class="fas fa-magic"></i> 音频处理</label>
                              <div class="processing-options">
                                  <div class="processing-option active" id="aecOption">
                                      <input type="checkbox" id="aecToggle" checked>
                                      <label for="aecToggle">回声消除</label>
                                  </div>
                                  <div class="processing-option active" id="ansOption">
                                      <input type="checkbox" id="ansToggle" checked>
                                      <label for="ansToggle">降噪</label>
                                  </div>
                                  <div class="processing-option active" id="agcOption">
                                      <input type="checkbox" id="agcToggle" checked>
                                      <label for="agcToggle">自动增益</label>
                                  </div>
                              </div>
                          </div>
                          
                          <div class="volume-meter" id="volumeMeter">
                              <div class="volume-level" id="volumeLevel" style="width: 0%"></div>
                          </div>
                          
                          <div class="device-actions">
                              <button class="refresh-btn" id="refreshDevices">
                                  <i class="fas fa-sync-alt"></i>
                                  <span>刷新设备列表</span>
                              </button>
                              <button class="test-btn" id="testMic">
                                  <i class="fas fa-play"></i>
                                  <span>测试麦克风</span>
                              </button>
                          </div>
                      </div>
                      
                      <div id="callStatus" class="call-status">
                          <i class="fas fa-circle-notch fa-spin"></i>
                          <span id="statusText">正在连接...</span>
                      </div>
                  </div>
                  
                  <!-- 成员列表 -->
                  <div class="members-container">
                      <div class="members-header">
                          <h2><i class="fas fa-users"></i> 通话成员</h2>
                          <div class="members-count">
                              <span id="memberCount">0</span> 人在线
                          </div>
                      </div>
                      
                      <div id="membersList" class="members-list">
                          <div class="empty-state">
                              <i class="fas fa-user-friends"></i>
                              <p>频道内暂无成员</p>
                              <p style="font-size: 12px; margin-top: 5px;">加入频道后，成员列表将在这里显示</p>
                          </div>
                      </div>
                  </div>
                  
                  <!-- 页脚 -->
                  <div class="footer">
                      <p>Powered by Agora Web SDK v4.21.0 | Cordova Android App</p>
                  </div>
              </div>

              <!-- 引入声网 Web SDK -->
              <script src="https://download.agora.io/sdk/release/AgoraRTC_N-4.21.0.js"></script>
              
              <!-- Cordova核心脚本 -->
              <script type="text/javascript" src="cordova.js"></script>
              
              <!-- 应用主脚本 -->
              <script>
                  // 全局变量
                  let rtcClient = null;
                  let localAudioTrack = null;
                  let localScreenAudioTrack = null;
                  let isPublished = false;
                  let isMicrophoneMuted = true;
                  let isInCall = false;
                  let members = new Map();
                  const APP_ID = "4531524ecba946fd868fc547c6d269d7";
                  
                  // 音频设备相关变量
                  let audioInputDevices = [];
                  let audioOutputDevices = [];
                  let selectedMicDeviceId = '';
                  let selectedSpeakerDeviceId = '';
                  let testAudioContext = null;
                  let testAudioAnalyser = null;
                  let testAudioStream = null;
                  let isTestingMic = false;
                  
                  // 媒体源和音频处理变量
                  let currentAudioSource = 'microphone';
                  let audioProcessingOptions = {
                      AEC: true,
                      ANS: true,
                      AGC: true
                  };
                  
                  // 页面加载完成后初始化
                  document.addEventListener('deviceready', onDeviceReady, false);
                  
                  function onDeviceReady() {
                      console.log('Cordova设备已就绪');
                      
                      // 初始化UI
                      initUI();
                      
                      // 请求权限
                      requestPermissions();
                      
                      // 初始化音频设备
                      setTimeout(initAudioDevices, 1000);
                  }
                  
                  function initUI() {
                      // 获取DOM元素
                      const callToggleBtn = document.getElementById('callToggle');
                      const micToggleBtn = document.getElementById('micToggle');
                      const callTextSpan = document.getElementById('callText');
                      const micTextSpan = document.getElementById('micText');
                      const callStatusDiv = document.getElementById('callStatus');
                      const statusTextSpan = document.getElementById('statusText');
                      const channelNameInput = document.getElementById('channelName');
                      const tokenInput = document.getElementById('tokenInput');
                      const membersListDiv = document.getElementById('membersList');
                      const memberCountSpan = document.getElementById('memberCount');
                      
                      // 获取设备选择相关DOM元素
                      const micSelect = document.getElementById('micSelect');
                      const speakerSelect = document.getElementById('speakerSelect');
                      const refreshDevicesBtn = document.getElementById('refreshDevices');
                      const testMicBtn = document.getElementById('testMic');
                      const micStatusDiv = document.getElementById('micStatus');
                      const speakerStatusDiv = document.getElementById('speakerStatus');
                      const volumeMeter = document.getElementById('volumeMeter');
                      const volumeLevel = document.getElementById('volumeLevel');
                      
                      // 绑定事件监听器
                      if (callToggleBtn) {
                          callToggleBtn.addEventListener('click', toggleCall);
                      }
                      
                      if (micToggleBtn) {
                          micToggleBtn.addEventListener('click', toggleMicrophone);
                      }
                      
                      if (micSelect) {
                          micSelect.addEventListener('change', onMicDeviceChange);
                      }
                      
                      if (speakerSelect) {
                          speakerSelect.addEventListener('change', onSpeakerDeviceChange);
                      }
                      
                      if (refreshDevicesBtn) {
                          refreshDevicesBtn.addEventListener('click', refreshAudioDevices);
                      }
                      
                      if (testMicBtn) {
                          testMicBtn.addEventListener('click', toggleMicTest);
                      }
                      
                      // 设置初始状态
                      updateMembersDisplay();
                      
                      console.log('UI初始化完成');
                  }
                  
                  function requestPermissions() {
                      console.log('请求Android权限');
                      
                      // 检查Android权限
                      if (typeof AndroidPermissions !== 'undefined') {
                          var permissions = cordova.plugins.permissions;
                          var permissionList = [
                              permissions.RECORD_AUDIO,
                              permissions.MODIFY_AUDIO_SETTINGS,
                              permissions.INTERNET,
                              permissions.ACCESS_NETWORK_STATE,
                              permissions.WAKE_LOCK
                          ];
                          
                          permissions.requestPermissions(permissionList, function(success) {
                              console.log('权限请求成功');
                          }, function(error) {
                              console.log('权限请求失败: ', error);
                          });
                      }
                  }
                  
                  async function initAudioDevices() {
                      console.log('初始化音频设备');
                      await refreshAudioDevices();
                      
                      // 监听设备变化
                      if (navigator.mediaDevices && navigator.mediaDevices.addEventListener) {
                          navigator.mediaDevices.addEventListener('devicechange', handleDeviceChange);
                      }
                  }
                  
                  // 刷新音频设备列表
                  async function refreshAudioDevices() {
                      const refreshDevicesBtn = document.getElementById('refreshDevices');
                      const micSelect = document.getElementById('micSelect');
                      const speakerSelect = document.getElementById('speakerSelect');
                      
                      if (!refreshDevicesBtn) return;
                      
                      refreshDevicesBtn.disabled = true;
                      refreshDevicesBtn.innerHTML = '<i class="fas fa-circle-notch fa-spin"></i> 获取中...';
                      
                      try {
                          // 检查麦克风权限
                          try {
                              await navigator.mediaDevices.getUserMedia({ audio: true });
                          } catch (err) {
                              console.warn('麦克风权限未授权:', err);
                              alert('请授予麦克风权限以使用音频功能');
                          }
                          
                          // 获取所有音频设备
                          const devices = await navigator.mediaDevices.enumerateDevices();
                          
                          // 分离输入和输出设备
                          audioInputDevices = devices.filter(device => device.kind === 'audioinput');
                          audioOutputDevices = devices.filter(device => device.kind === 'audiooutput');
                          
                          console.log('音频输入设备:', audioInputDevices);
                          console.log('音频输出设备:', audioOutputDevices);
                          
                          // 更新麦克风选择下拉框
                          updateDeviceSelect(micSelect, audioInputDevices, selectedMicDeviceId, '使用默认麦克风');
                          
                          // 更新扬声器选择下拉框
                          updateDeviceSelect(speakerSelect, audioOutputDevices, selectedSpeakerDeviceId, '使用默认扬声器');
                          
                          // 更新设备状态显示
                          updateDeviceStatus();
                          
                          // 启用选择框
                          micSelect.disabled = audioInputDevices.length === 0;
                          speakerSelect.disabled = audioOutputDevices.length === 0;
                          
                      } catch (error) {
                          console.error('获取音频设备失败:', error);
                          showDeviceError('无法获取音频设备列表');
                      } finally {
                          refreshDevicesBtn.disabled = false;
                          refreshDevicesBtn.innerHTML = '<i class="fas fa-sync-alt"></i> 刷新设备列表';
                      }
                  }
                  
                  // 更新设备选择下拉框
                  function updateDeviceSelect(selectElement, devices, selectedId, defaultText) {
                      if (!selectElement) return;
                      
                      // 清空现有选项
                      selectElement.innerHTML = '';
                      
                      if (devices.length === 0) {
                          selectElement.innerHTML = `<option value="">未检测到设备</option>`;
                          return;
                      }
                      
                      // 添加默认选项
                      const defaultOption = document.createElement('option');
                      defaultOption.value = '';
                      defaultOption.textContent = defaultText;
                      selectElement.appendChild(defaultOption);
                      
                      // 添加设备选项
                      devices.forEach(device => {
                          const option = document.createElement('option');
                          option.value = device.deviceId;
                          
                          let deviceName = device.label || `音频设备 ${device.deviceId.substring(0, 8)}...`;
                          
                          if (deviceName.includes('Default') || deviceName.includes('默认')) {
                              deviceName = deviceName.replace(/\(.*?\)/g, '').trim();
                          }
                          
                          option.textContent = deviceName;
                          
                          if (device.deviceId === selectedId) {
                              option.selected = true;
                          }
                          
                          selectElement.appendChild(option);
                      });
                  }
                  
                  // 更新设备状态显示
                  function updateDeviceStatus() {
                      const micStatusDiv = document.getElementById('micStatus');
                      const speakerStatusDiv = document.getElementById('speakerStatus');
                      
                      if (!micStatusDiv || !speakerStatusDiv) return;
                      
                      // 麦克风状态
                      if (selectedMicDeviceId && audioInputDevices.some(d => d.deviceId === selectedMicDeviceId)) {
                          const selectedMic = audioInputDevices.find(d => d.deviceId === selectedMicDeviceId);
                          let deviceName = selectedMic.label || '麦克风';
                          deviceName = deviceName.replace(/\(.*?\)/g, '').trim();
                          micStatusDiv.className = 'device-status active';
                          micStatusDiv.innerHTML = `<i class="fas fa-check-circle"></i> 已选择: ${deviceName.substring(0, 30)}`;
                      } else {
                          micStatusDiv.className = 'device-status inactive';
                          micStatusDiv.innerHTML = `<i class="fas fa-circle"></i> 使用默认麦克风`;
                      }
                      
                      // 扬声器状态
                      if (selectedSpeakerDeviceId && audioOutputDevices.some(d => d.deviceId === selectedSpeakerDeviceId)) {
                          const selectedSpeaker = audioOutputDevices.find(d => d.deviceId === selectedSpeakerDeviceId);
                          let deviceName = selectedSpeaker.label || '扬声器';
                          deviceName = deviceName.replace(/\(.*?\)/g, '').trim();
                          speakerStatusDiv.className = 'device-status active';
                          speakerStatusDiv.innerHTML = `<i class="fas fa-check-circle"></i> 已选择: ${deviceName.substring(0, 30)}`;
                      } else {
                          speakerStatusDiv.className = 'device-status inactive';
                          speakerStatusDiv.innerHTML = `<i class="fas fa-circle"></i> 使用默认扬声器`;
                      }
                  }
                  
                  // 麦克风设备变更处理
                  async function onMicDeviceChange(event) {
                      selectedMicDeviceId = event.target.value;
                      updateDeviceStatus();
                      
                      // 如果在通话中，重新创建音频轨道
                      if (isInCall && localAudioTrack) {
                          await switchMicrophoneDevice();
                      }
                  }
                  
                  // 扬声器设备变更处理
                  async function onSpeakerDeviceChange(event) {
                      selectedSpeakerDeviceId = event.target.value;
                      updateDeviceStatus();
                      
                      // 设置音频输出设备
                      await setAudioOutputDevice();
                  }
                  
                  // 设备变化监听
                  function handleDeviceChange() {
                      console.log('检测到音频设备变化，重新获取设备列表');
                      refreshAudioDevices();
                  }
                  
                  // 显示设备错误
                  function showDeviceError(message) {
                      const micSelect = document.getElementById('micSelect');
                      const speakerSelect = document.getElementById('speakerSelect');
                      
                      if (micSelect) {
                          micSelect.innerHTML = `<option value="">${message}</option>`;
                          micSelect.disabled = true;
                      }
                      
                      if (speakerSelect) {
                          speakerSelect.innerHTML = `<option value="">${message}</option>`;
                          speakerSelect.disabled = true;
                      }
                  }
                  
                  // 切换麦克风设备
                  async function switchMicrophoneDevice() {
                      if (!isInCall || !localAudioTrack) {
                          return;
                      }
                      
                      try {
                          console.log('切换麦克风设备:', selectedMicDeviceId);
                          
                          // 停止当前的麦克风测试
                          stopMicTest();
                          
                          // 重新创建音频轨道
                          await switchAudioSource(currentAudioSource);
                          
                          console.log('麦克风设备切换成功');
                          updateCallStatus('connected', '麦克风设备已切换');
                          
                      } catch (error) {
                          console.error('切换麦克风设备失败:', error);
                          updateCallStatus('error', `麦克风切换失败: ${error.message}`);
                          
                          // 恢复原设备选择
                          const micSelect = document.getElementById('micSelect');
                          if (micSelect) {
                              micSelect.value = '';
                          }
                          selectedMicDeviceId = '';
                          updateDeviceStatus();
                      }
                  }
                  
                  // 设置音频输出设备
                  async function setAudioOutputDevice() {
                      if (!selectedSpeakerDeviceId) {
                          try {
                              if (localAudioTrack && localAudioTrack.setPlaybackDevice) {
                                  await localAudioTrack.setPlaybackDevice('default');
                              }
                              
                              members.forEach(member => {
                                  if (member.audioTrack && !member.isLocal && member.audioTrack.setPlaybackDevice) {
                                      member.audioTrack.setPlaybackDevice('default');
                                  }
                              });
                              
                              console.log('音频输出设备已恢复为默认');
                          } catch (error) {
                              console.warn('设置默认音频输出设备失败:', error);
                          }
                          return;
                      }
                      
                      try {
                          if (localAudioTrack && localAudioTrack.setPlaybackDevice) {
                              await localAudioTrack.setPlaybackDevice(selectedSpeakerDeviceId);
                          }
                          
                          members.forEach(member => {
                              if (member.audioTrack && !member.isLocal && member.audioTrack.setPlaybackDevice) {
                                  member.audioTrack.setPlaybackDevice(selectedSpeakerDeviceId);
                              }
                          });
                          
                          console.log('音频输出设备已切换:', selectedSpeakerDeviceId);
                          
                      } catch (error) {
                          console.error('切换音频输出设备失败:', error);
                          const speakerSelect = document.getElementById('speakerSelect');
                          if (speakerSelect) {
                              speakerSelect.value = '';
                          }
                          selectedSpeakerDeviceId = '';
                          updateDeviceStatus();
                      }
                  }
                  
                  // 切换麦克风测试
                  async function toggleMicTest() {
                      if (isTestingMic) {
                          stopMicTest();
                      } else {
                          await startMicTest();
                      }
                  }
                  
                  // 开始麦克风测试
                  async function startMicTest() {
                      const testMicBtn = document.getElementById('testMic');
                      
                      if (!selectedMicDeviceId && audioInputDevices.length > 0) {
                          selectedMicDeviceId = audioInputDevices[0].deviceId;
                          const micSelect = document.getElementById('micSelect');
                          if (micSelect) {
                              micSelect.value = selectedMicDeviceId;
                          }
                          updateDeviceStatus();
                      }
                      
                      if (!selectedMicDeviceId) {
                          alert('请先选择一个麦克风设备进行测试');
                          return;
                      }
                      
                      testMicBtn.disabled = true;
                      testMicBtn.innerHTML = '<i class="fas fa-circle-notch fa-spin"></i> 准备中...';
                      
                      try {
                          const constraints = {
                              audio: {
                                  deviceId: selectedMicDeviceId ? { exact: selectedMicDeviceId } : undefined,
                                  echoCancellation: false,
                                  noiseSuppression: false,
                                  autoGainControl: false
                              }
                          };
                          
                          testAudioStream = await navigator.mediaDevices.getUserMedia(constraints);
                          
                          testAudioContext = new (window.AudioContext || window.webkitAudioContext)();
                          const source = testAudioContext.createMediaStreamSource(testAudioStream);
                          testAudioAnalyser = testAudioContext.createAnalyser();
                          
                          source.connect(testAudioAnalyser);
                          testAudioAnalyser.fftSize = 256;
                          
                          const volumeMeter = document.getElementById('volumeMeter');
                          const volumeLevel = document.getElementById('volumeLevel');
                          
                          if (volumeMeter) volumeMeter.style.display = 'block';
                          if (volumeLevel) volumeLevel.style.width = '0%';
                          
                          isTestingMic = true;
                          testMicBtn.disabled = false;
                          testMicBtn.innerHTML = '<i class="fas fa-stop"></i> 停止测试';
                          
                          updateVolumeMeter();
                          
                          console.log('麦克风测试开始');
                          
                      } catch (error) {
                          console.error('麦克风测试失败:', error);
                          alert(`麦克风测试失败: ${error.message}`);
                          testMicBtn.disabled = false;
                          testMicBtn.innerHTML = '<i class="fas fa-play"></i> 测试麦克风';
                      }
                  }
                  
                  // 停止麦克风测试
                  function stopMicTest() {
                      if (testAudioContext) {
                          testAudioContext.close();
                          testAudioContext = null;
                          testAudioAnalyser = null;
                      }
                      
                      if (testAudioStream) {
                          testAudioStream.getTracks().forEach(track => track.stop());
                          testAudioStream = null;
                      }
                      
                      const volumeMeter = document.getElementById('volumeMeter');
                      const volumeLevel = document.getElementById('volumeLevel');
                      const testMicBtn = document.getElementById('testMic');
                      
                      if (volumeMeter) volumeMeter.style.display = 'none';
                      if (volumeLevel) volumeLevel.style.width = '0%';
                      
                      isTestingMic = false;
                      if (testMicBtn) {
                          testMicBtn.disabled = false;
                          testMicBtn.innerHTML = '<i class="fas fa-play"></i> 测试麦克风';
                      }
                      
                      console.log('麦克风测试停止');
                  }
                  
                  // 更新音量显示
                  function updateVolumeMeter() {
                      if (!isTestingMic || !testAudioAnalyser) return;
                      
                      const dataArray = new Uint8Array(testAudioAnalyser.frequencyBinCount);
                      testAudioAnalyser.getByteFrequencyData(dataArray);
                      
                      let sum = 0;
                      for (let i = 0; i < dataArray.length; i++) {
                          sum += dataArray[i];
                      }
                      const average = sum / dataArray.length;
                      const volume = Math.min(100, Math.round((average / 128) * 100));
                      
                      const volumeLevel = document.getElementById('volumeLevel');
                      if (volumeLevel) {
                          volumeLevel.style.width = volume + '%';
                      }
                      
                      requestAnimationFrame(updateVolumeMeter);
                  }
                  
                  // 添加成员
                  function addMember(uid, isLocal = false, audioTrack = null) {
                      if (members.has(uid.toString())) {
                          const existingMember = members.get(uid.toString());
                          existingMember.audioTrack = audioTrack;
                          existingMember.isMuted = false;
                      } else {
                          const member = {
                              uid: uid,
                              isLocal: isLocal,
                              audioTrack: audioTrack,
                              isMuted: false,
                              isSpeaking: false
                          };
                          members.set(uid.toString(), member);
                      }
                  }
                  
                  // 移除成员
                  function removeMember(uid) {
                      members.delete(uid.toString());
                  }
                  
                  // 更新成员显示
                  function updateMembersDisplay() {
                      const membersListDiv = document.getElementById('membersList');
                      const memberCountSpan = document.getElementById('memberCount');
                      
                      if (!membersListDiv || !memberCountSpan) {
                          console.warn('成员列表元素未找到');
                          return;
                      }
                      
                      if (members.size === 0) {
                          membersListDiv.innerHTML = `
                              <div class="empty-state">
                                  <i class="fas fa-user-friends"></i>
                                  <p>频道内暂无成员</p>
                                  <p style="font-size: 12px; margin-top: 5px;">加入频道后，成员列表将在这里显示</p>
                              </div>
                          `;
                          memberCountSpan.textContent = "0";
                          return;
                      }
                      
                      memberCountSpan.textContent = members.size.toString();
                      
                      let membersHTML = "";
                      
                      members.forEach(member => {
                          const isLocal = member.isLocal;
                          const uid = member.uid;
                          
                          const colors = ['#667eea', '#764ba2', '#4CAF50', '#FF9800', '#E91E63', '#9C27B0'];
                          const colorIndex = parseInt(uid) % colors.length || 0;
                          
                          let statusClass = "status-online";
                          let statusText = "在线";
                          let statusIcon = "fa-circle";
                          
                          let isActuallyMuted = member.isMuted;
                          if (isLocal) {
                              isActuallyMuted = isMicrophoneMuted;
                          }
                          
                          if (isActuallyMuted) {
                              statusClass = "status-muted";
                              statusText = "已静音";
                              statusIcon = "fa-microphone-slash";
                          } else if (member.isSpeaking) {
                              statusClass = "status-speaker";
                              statusText = "正在发言";
                              statusIcon = "fa-volume-up";
                          }
                          
                          const muteIcon = isActuallyMuted ? 'fa-microphone-slash' : 'fa-microphone';
                          const muteTitle = isActuallyMuted ? '取消静音' : '静音';
                          const muteBtnClass = isActuallyMuted ? 'mute-btn muted' : 'mute-btn';
                          
                          membersHTML += `
                              <div class="member-card ${isLocal ? 'local' : ''}">
                                  <div class="avatar ${isLocal ? 'local' : 'remote'}" style="background: ${colors[colorIndex]}">
                                      ${isLocal ? '我' : '用户'}
                                  </div>
                                  <div class="member-info">
                                      <div class="member-name">
                                          ${isLocal ? '我 (本地)' : `用户 ${uid}`}
                                      </div>
                                      <div class="member-uid">UID: ${uid}</div>
                                      <div class="member-status ${statusClass}">
                                          <i class="fas ${statusIcon}"></i>
                                          ${statusText}
                                      </div>
                                  </div>
                                  <div class="member-actions">
                                      <button class="action-btn ${muteBtnClass}" 
                                              onclick="window.toggleMemberMute('${uid}')" 
                                              title="${muteTitle}"
                                              ${isLocal ? 'disabled' : ''}>
                                          <i class="fas ${muteIcon}"></i>
                                      </button>
                                  </div>
                              </div>
                          `;
                      });
                      
                      membersListDiv.innerHTML = membersHTML;
                  }
                  
                  // 切换成员静音
                  window.toggleMemberMute = function(uid) {
                      const member = members.get(uid);
                      if (!member) return;
                      
                      if (member.isLocal) {
                          toggleMicrophone();
                      } else {
                          if (member.audioTrack) {
                              if (member.isMuted) {
                                  member.audioTrack.play();
                              } else {
                                  member.audioTrack.stop();
                              }
                              member.isMuted = !member.isMuted;
                              updateMembersDisplay();
                          }
                      }
                  };
                  
                  // 检测麦克风权限
                  async function checkMicPermission() {
                      try {
                          const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
                          stream.getTracks().forEach(track => track.stop());
                          return true;
                      } catch (e) {
                          alert(`麦克风权限检测失败：${e.message}\n请检查设备权限设置`);
                          console.error("麦克风权限失败：", e);
                          return false;
                      }
                  }
                  
                  // 更新通话状态显示
                  function updateCallStatus(status, message) {
                      const callStatusDiv = document.getElementById('callStatus');
                      const statusTextSpan = document.getElementById('statusText');
                      
                      if (!callStatusDiv || !statusTextSpan) return;
                      
                      callStatusDiv.style.display = 'block';
                      callStatusDiv.className = 'call-status';
                      
                      if (status === 'connecting') {
                          statusTextSpan.innerHTML = `<i class="fas fa-circle-notch fa-spin"></i> ${message}`;
                      } else if (status === 'connected') {
                          callStatusDiv.classList.add('connected');
                          statusTextSpan.innerHTML = `<i class="fas fa-check-circle"></i> ${message}`;
                      } else if (status === 'disconnected') {
                          callStatusDiv.classList.add('disconnected');
                          statusTextSpan.innerHTML = `<i class="fas fa-times-circle"></i> ${message}`;
                      } else if (status === 'error') {
                          callStatusDiv.classList.add('disconnected');
                          statusTextSpan.innerHTML = `<i class="fas fa-exclamation-circle"></i> ${message}`;
                      }
                      
                      if (status === 'connected' || status === 'error') {
                          setTimeout(() => {
                              if (callStatusDiv && (callStatusDiv.classList.contains('connected') || 
                                   callStatusDiv.classList.contains('disconnected'))) {
                                  callStatusDiv.style.display = 'none';
                              }
                          }, 5000);
                      }
                  }
                  
                  // 切换音频源
                  async function switchAudioSource(sourceType) {
                      if (!isInCall) {
                          return;
                      }
                      
                      try {
                          console.log('切换音频源到:', sourceType);
                          
                          // 停止当前的麦克风测试
                          stopMicTest();
                          
                          // 关闭当前轨道
                          if (localAudioTrack) {
                              // 先取消发布
                              if (isPublished) {
                                  await rtcClient.unpublish([localAudioTrack]);
                              }
                              
                              localAudioTrack.close();
                              localAudioTrack = null;
                          }
                          
                          // 关闭屏幕音频轨道
                          if (localScreenAudioTrack) {
                              localScreenAudioTrack.close();
                              localScreenAudioTrack = null;
                          }
                          
                          // 创建新音频轨道
                          let newAudioTrack;
                          
                          if (sourceType === 'microphone') {
                              const microphoneConfig = {};
                              
                              if (selectedMicDeviceId) {
                                  microphoneConfig.microphoneId = selectedMicDeviceId;
                              }
                              
                              // 添加音频处理配置
                              Object.assign(microphoneConfig, {
                                  encoderConfig: 'music_standard',
                                  AEC: audioProcessingOptions.AEC,
                                  ANS: audioProcessingOptions.ANS,
                                  AGC: audioProcessingOptions.AGC
                              });
                              
                              newAudioTrack = await AgoraRTC.createMicrophoneAudioTrack(microphoneConfig);
                          }
                          
                          if (!newAudioTrack) {
                              throw new Error('音频轨道创建失败');
                          }
                          
                          // 设置新轨道
                          localAudioTrack = newAudioTrack;
                          
                          // 重新发布
                          if (isPublished) {
                              await rtcClient.publish([localAudioTrack]);
                              
                              // 更新静音状态
                              localAudioTrack.setEnabled(!isMicrophoneMuted);
                          } else {
                              localAudioTrack.setEnabled(!isMicrophoneMuted);
                          }
                          
                          // 更新本地成员的音频轨道引用
                          const localMember = Array.from(members.values()).find(m => m.isLocal);
                          if (localMember) {
                              localMember.audioTrack = localAudioTrack;
                          }
                          
                          console.log('音频源切换成功');
                          updateCallStatus('connected', `已切换到麦克风`);
                          
                      } catch (error) {
                          console.error('切换音频源失败:', error);
                          updateCallStatus('error', `音频源切换失败: ${error.message}`);
                      }
                  }
                  
                  // 初始化客户端
                  async function initRtcClient() {
                      if (rtcClient) return;
                      
                      try {
                          rtcClient = AgoraRTC.createClient({ mode: "rtc", codec: "vp8" });
                          
                          // 监听远程用户加入
                          rtcClient.on("user-published", async (user, mediaType) => {
                              try {
                                  await rtcClient.subscribe(user, mediaType);
                                  if (mediaType === "audio") {
                                      const remoteAudioTrack = user.audioTrack;
                                      remoteAudioTrack.play();
                                      
                                      // 添加远程成员到列表
                                      addMember(user.uid, false, remoteAudioTrack);
                                      updateMembersDisplay();
                                      console.log(`已订阅远程用户 ${user.uid} 的音频`);
                                  }
                              } catch (subscribeErr) {
                                  console.error("订阅远程用户失败：", subscribeErr);
                              }
                          });
                          
                          // 监听用户取消发布
                          rtcClient.on("user-unpublished", (user, mediaType) => {
                              const member = members.get(user.uid.toString());
                              if (member && !member.isLocal) {
                                  member.isMuted = true;
                                  updateMembersDisplay();
                                  console.log(`用户 ${user.uid} 已关闭麦克风`);
                              }
                          });
                          
                          // 监听用户离开频道
                          rtcClient.on("user-left", (user) => {
                              removeMember(user.uid);
                              updateMembersDisplay();
                              console.log(`用户 ${user.uid} 已离开频道`);
                          });
                          
                          // 监听用户重新发布媒体流
                          rtcClient.on("user-published", async (user, mediaType) => {
                              if (mediaType === "audio" && members.has(user.uid.toString())) {
                                  try {
                                      await rtcClient.subscribe(user, mediaType);
                                      const remoteAudioTrack = user.audioTrack;
                                      remoteAudioTrack.play();
                                      
                                      const member = members.get(user.uid.toString());
                                      if (member) {
                                          member.audioTrack = remoteAudioTrack;
                                          member.isMuted = false;
                                          updateMembersDisplay();
                                          console.log(`用户 ${user.uid} 重新开启麦克风`);
                                      }
                                  } catch (subscribeErr) {
                                      console.error("重新订阅用户失败：", subscribeErr);
                                  }
                              }
                          });
                          
                      } catch (initErr) {
                          console.error("客户端初始化失败：", initErr);
                          throw initErr;
                      }
                  }
                  
                  // 切换通话状态
                  async function toggleCall() {
                      const callToggleBtn = document.getElementById('callToggle');
                      const micToggleBtn = document.getElementById('micToggle');
                      
                      if (!callToggleBtn || !micToggleBtn) return;
                      
                      if (isInCall) {
                          await leaveChannel();
                      } else {
                          await joinChannel();
                      }
                  }
                  
                  // 加入频道
                  async function joinChannel() {
                      const channelNameInput = document.getElementById('channelName');
                      const callToggleBtn = document.getElementById('callToggle');
                      const micToggleBtn = document.getElementById('micToggle');
                      const micTextSpan = document.getElementById('micText');
                      const callTextSpan = document.getElementById('callText');
                      
                      if (!channelNameInput) {
                          alert("无法获取频道输入框");
                          return;
                      }
                      
                      const channelName = channelNameInput.value.trim();
                      if (!channelName) {
                          alert("请输入有效的频道名！");
                          return;
                      }
                      
                      // 检测麦克风权限
                      const hasMicPerm = await checkMicPermission();
                      if (!hasMicPerm) return;
                      
                      updateCallStatus('connecting', '正在加入频道...');
                      if (callToggleBtn) callToggleBtn.disabled = true;
                      
                      try {
                          await initRtcClient();
                          
                          // 创建音频轨道
                          const microphoneConfig = {};
                          
                          if (selectedMicDeviceId) {
                              microphoneConfig.microphoneId = selectedMicDeviceId;
                          }
                          
                          // 添加音频处理配置
                          Object.assign(microphoneConfig, {
                              encoderConfig: 'music_standard',
                              AEC: audioProcessingOptions.AEC,
                              ANS: audioProcessingOptions.ANS,
                              AGC: audioProcessingOptions.AGC
                          });
                          
                          const audioTrack = await AgoraRTC.createMicrophoneAudioTrack(microphoneConfig);
                          
                          if (!audioTrack || !audioTrack.play) {
                              throw new Error("音频轨道创建失败：非有效轨道实例");
                          }
                          
                          localAudioTrack = audioTrack;
                          console.log(`音频轨道创建成功`);
                          
                          // 获取用户输入的Token
                          const tokenInput = document.getElementById('tokenInput');
                          const userToken = tokenInput ? tokenInput.value.trim() : '';
                          const token = userToken || null;
                          
                          // 加入频道
                          const uid = await rtcClient.join(APP_ID, channelName, token);
                          console.log(`加入频道【${channelName}】成功，UID: ${uid}`);
                          
                          // 添加本地成员到列表
                          addMember(uid, true, localAudioTrack);
                          updateMembersDisplay();
                          
                          // 发布音频
                          if (!isPublished) {
                              await rtcClient.publish([localAudioTrack]);
                              isPublished = true;
                              console.log("本地音频发布成功");
                              
                              if (micTextSpan) micTextSpan.textContent = '关闭话筒';
                              if (micToggleBtn) {
                                  micToggleBtn.classList.add('muted');
                                  const micIcon = micToggleBtn.querySelector('i');
                                  if (micIcon) micIcon.className = 'fas fa-microphone-slash';
                              }
                              
                              localAudioTrack.setEnabled(true);
                              isMicrophoneMuted = false;
                              
                              const localMember = Array.from(members.values()).find(m => m.isLocal);
                              if (localMember) {
                                  localMember.isMuted = false;
                                  updateMembersDisplay();
                              }
                          }
                          
                          // 设置音频输出设备
                          await setAudioOutputDevice();
                          
                          // 更新通话状态
                          isInCall = true;
                          
                          // 更新按钮状态
                          if (callToggleBtn) {
                              callToggleBtn.disabled = false;
                              callToggleBtn.classList.add('in-call');
                              if (callTextSpan) callTextSpan.textContent = '退出通话';
                              const callIcon = callToggleBtn.querySelector('i');
                              if (callIcon) callIcon.className = 'fas fa-phone-slash';
                          }
                          
                          // 启用话筒按钮
                          if (micToggleBtn) micToggleBtn.disabled = false;
                          
                          // 更新状态显示
                          updateCallStatus('connected', `已加入频道：${channelName} | UID：${uid}`);
                          
                      } catch (e) {
                          console.error("加入/发布失败（详细信息）：", {
                              message: e.message,
                              code: e.code,
                              stack: e.stack
                          });
                          
                          let errMsg = `加入通话失败：${e.message}`;
                          if (e.code === "INVALID_PARAMS") {
                              errMsg += "\n原因：发布的轨道非有效本地轨道";
                          } else if (e.code === "CAN_NOT_GET_GATEWAY_SERVER") {
                              errMsg += "\n原因：Token/频道名/AppID 不匹配";
                          } else if (e.message.includes("permission")) {
                              errMsg += "\n原因：麦克风权限被拒绝";
                          } else if (e.message.includes("token")) {
                              errMsg += "\n原因：Token无效或过期";
                          }
                          
                          if (callToggleBtn) callToggleBtn.disabled = false;
                          isInCall = false;
                          
                          updateCallStatus('error', errMsg);
                          
                          if (localAudioTrack) {
                              localAudioTrack.close();
                              localAudioTrack = null;
                          }
                          isPublished = false;
                      }
                  }
                  
                  // 切换麦克风开关
                  function toggleMicrophone() {
                      const micToggleBtn = document.getElementById('micToggle');
                      const micTextSpan = document.getElementById('micText');
                      
                      if (!localAudioTrack || !micToggleBtn || !micTextSpan) return;
                      
                      if (isMicrophoneMuted) {
                          localAudioTrack.setEnabled(true);
                          const micIcon = micToggleBtn.querySelector('i');
                          if (micIcon) micIcon.className = 'fas fa-microphone-slash';
                          micTextSpan.textContent = '关闭话筒';
                          micToggleBtn.classList.add('muted');
                          console.log("麦克风已开启");
                          
                          const localMember = Array.from(members.values()).find(m => m.isLocal);
                          if (localMember) {
                              localMember.isMuted = false;
                          }
                      } else {
                          localAudioTrack.setEnabled(false);
                          const micIcon = micToggleBtn.querySelector('i');
                          if (micIcon) micIcon.className = 'fas fa-microphone';
                          micTextSpan.textContent = '开启话筒';
                          micToggleBtn.classList.remove('muted');
                          console.log("麦克风已关闭");
                          
                          const localMember = Array.from(members.values()).find(m => m.isLocal);
                          if (localMember) {
                              localMember.isMuted = true;
                          }
                      }
                      
                      isMicrophoneMuted = !isMicrophoneMuted;
                      updateMembersDisplay();
                  }
                  
                  // 退出频道
                  async function leaveChannel() {
                      const callToggleBtn = document.getElementById('callToggle');
                      const micToggleBtn = document.getElementById('micToggle');
                      const callTextSpan = document.getElementById('callText');
                      const micTextSpan = document.getElementById('micText');
                      
                      try {
                          stopMicTest();
                          
                          updateCallStatus('connecting', '正在离开频道...');
                          if (callToggleBtn) callToggleBtn.disabled = true;
                          
                          if (localAudioTrack) {
                              localAudioTrack.close();
                              localAudioTrack = null;
                              console.log("本地音频轨道已释放");
                          }
                          
                          if (localScreenAudioTrack) {
                              localScreenAudioTrack.close();
                              localScreenAudioTrack = null;
                              console.log("屏幕音频轨道已释放");
                          }
                          
                          if (rtcClient) {
                              await rtcClient.leave();
                              rtcClient = null;
                              console.log("已退出频道");
                          }
                          
                          members.clear();
                          updateMembersDisplay();
                          
                          isPublished = false;
                          isMicrophoneMuted = true;
                          isInCall = false;
                          
                          if (callToggleBtn) {
                              callToggleBtn.disabled = false;
                              callToggleBtn.classList.remove('in-call');
                              if (callTextSpan) callTextSpan.textContent = '加入通话';
                              const callIcon = callToggleBtn.querySelector('i');
                              if (callIcon) callIcon.className = 'fas fa-phone-alt';
                          }
                          
                          if (micToggleBtn) {
                              micToggleBtn.disabled = true;
                              const micIcon = micToggleBtn.querySelector('i');
                              if (micIcon) micIcon.className = 'fas fa-microphone';
                              if (micTextSpan) micTextSpan.textContent = '开启话筒';
                              micToggleBtn.classList.remove('muted');
                          }
                          
                          updateCallStatus('disconnected', '已退出通话');
                          
                      } catch (leaveErr) {
                          console.error("退出频道失败：", leaveErr);
                          updateCallStatus('error', `退出通话失败：${leaveErr.message}`);
                          if (callToggleBtn) callToggleBtn.disabled = false;
                      }
                  }
                  
                  // 让函数在全局可用
                  window.toggleCall = toggleCall;
                  window.toggleMicrophone = toggleMicrophone;
              </script>
          </body>
          </html>
          EOF
          
          # 创建config.xml
          cat > config.xml << 'EOF'
      <?xml version='1.0' encoding='utf-8'?>
      <widget id="com.example.agoraaudio" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0" xmlns:android="http://schemas.android.com/apk/res/android">
          <name>AgoraAudioCall</name>
          <description>
              声网音频通话应用，支持多人语音会议
          </description>
          <author email="dev@example.com" href="https://example.com">
              Agora Audio Team
          </author>
          <content src="index.html" />
          <access origin="*" />
          <allow-intent href="http://*/*" />
          <allow-intent href="https://*/*" />
          <allow-intent href="tel:*" />
          <allow-intent href="sms:*" />
          <allow-intent href="mailto:*" />
          <allow-intent href="geo:*" />
          
          <!-- Android权限 -->
          <platform name="android">
              <preference name="android-minSdkVersion" value="21" />
              <preference name="android-targetSdkVersion" value="29" />
              <preference name="Orientation" value="portrait" />
              <preference name="Fullscreen" value="false" />
              
              <!-- 权限配置 -->
              <config-file parent="/*" target="AndroidManifest.xml">
                  <uses-permission android:name="android.permission.RECORD_AUDIO" />
                  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
                  <uses-permission android:name="android.permission.INTERNET" />
                  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
                  <uses-permission android:name="android.permission.WAKE_LOCK" />
                  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
                  
                  <!-- WebRTC所需权限 -->
                  <uses-permission android:name="android.permission.CAMERA" />
                  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
                  
                  <!-- 允许混合内容（HTTP在HTTPS中） -->
                  <application android:usesCleartextTraffic="true">
                      <meta-data android:name="android.webkit.WebView.EnableSafeBrowsing" android:value="false" />
                  </application>
              </config-file>
          </platform>
          
          <!-- 插件配置 -->
          <plugin name="cordova-plugin-whitelist" spec="1" />
          <plugin name="cordova-plugin-android-permissions" spec="^1.1.5" />
          <plugin name="cordova-plugin-webrtc" spec="^1.0.0" />
          
          <!-- 图标和启动画面 -->
          <icon src="www/img/icon.png" />
          <icon src="www/img/icon-ldpi.png" platform="android" density="ldpi" />
          <icon src="www/img/icon-mdpi.png" platform="android" density="mdpi" />
          <icon src="www/img/icon-hdpi.png" platform="android" density="hdpi" />
          <icon src="www/img/icon-xhdpi.png" platform="android" density="xhdpi" />
          <icon src="www/img/icon-xxhdpi.png" platform="android" density="xxhdpi" />
          <icon src="www/img/icon-xxxhdpi.png" platform="android" density="xxxhdpi" />
          
          <splash src="www/img/splash.png" />
          <splash src="www/img/splash.png" platform="android" density="land-ldpi" />
          <splash src="www/img/splash.png" platform="android" density="land-mdpi" />
          <splash src="www/img/splash.png" platform="android" density="land-hdpi" />
          <splash src="www/img/splash.png" platform="android" density="land-xhdpi" />
          <splash src="www/img/splash.png" platform="android" density="land-xxhdpi" />
          <splash src="www/img/splash.png" platform="android" density="land-xxxhdpi" />
          <splash src="www/img/splash.png" platform="android" density="port-ldpi" />
          <splash src="www/img/splash.png" platform="android" density="port-mdpi" />
          <splash src="www/img/splash.png" platform="android" density="port-hdpi" />
          <splash src="www/img/splash.png" platform="android" density="port-xhdpi" />
          <splash src="www/img/splash.png" platform="android" density="port-xxhdpi" />
          <splash src="www/img/splash.png" platform="android" density="port-xxxhdpi" />
      </widget>
      EOF
          
          # 创建package.json
          cat > package.json << 'EOF'
          {
            "name": "com.example.agoraaudio",
            "displayName": "AgoraAudioCall",
            "version": "1.0.0",
            "description": "声网音频通话应用，支持多人语音会议",
            "main": "index.js",
            "scripts": {
              "test": "echo \"Error: no test specified\" && exit 1"
            },
            "keywords": [
              "ecosystem:cordova",
              "agora",
              "audio",
              "voice",
              "call"
            ],
            "author": "Agora Audio Team",
            "license": "Apache-2.0",
            "dependencies": {
              "cordova-android": "^8.1.0",
              "cordova-plugin-android-permissions": "^1.1.5",
              "cordova-plugin-webrtc": "^1.0.0"
            },
            "devDependencies": {},
            "cordova": {
              "plugins": {
                "cordova-plugin-whitelist": {},
                "cordova-plugin-android-permissions": {},
                "cordova-plugin-webrtc": {}
              },
              "platforms": [
                "android"
              ]
            }
          }
          EOF
          
          # 创建www/js/app.js
          cat > www/js/app.js << 'EOF'
          // Cordova应用初始化
          var app = {
              initialize: function() {
                  document.addEventListener('deviceready', this.onDeviceReady.bind(this), false);
              },
              
              onDeviceReady: function() {
                  console.log('Device is ready');
                  this.receivedEvent('deviceready');
                  
                  // 设置状态栏（如果插件可用）
                  if (window.StatusBar) {
                      StatusBar.styleDefault();
                  }
                  
                  // 设置屏幕方向
                  if (screen && screen.orientation) {
                      screen.orientation.lock('portrait');
                  }
              },
              
              receivedEvent: function(id) {
                  console.log('Received Event: ' + id);
              }
          };
          
          app.initialize();
          EOF
          
          sudo chmod -R 777 platforms/android/
          sudo chown -R runner:runner platforms/android/

      # 7. 修复CordovaLib Repository
      - name: Fix CordovaLib Repository (Correct Scope)
        run: |
          cd my-app/platforms/android/CordovaLib
          
          # ========== 修复1：buildscript的repositories块 ==========
          if grep -q "buildscript {.*repositories {" build.gradle; then
              sed -i '/buildscript {.*repositories {/a \            maven { url "https://maven.aliyun.com/repository/jcenter" }' build.gradle
          elif grep -q "buildscript {" build.gradle; then
              sed -i '/buildscript {/a \    repositories {\n        maven { url "https://maven.aliyun.com/repository/jcenter" }\n        jcenter()\n        google()\n    }' build.gradle
          else
              sed -i '1i buildscript {\n    repositories {\n        maven { url "https://maven.aliyun.com/repository/jcenter" }\n        jcenter()\n        google()\n    }\n    dependencies {\n        classpath "com.android.tools.build:gradle:3.4.0"\n    }\n}' build.gradle
          fi
          
          # ========== 修复2：普通repositories块 ==========
          if grep -q "repositories {" build.gradle; then
              sed -i '/repositories {/a \        maven { url "https://maven.aliyun.com/repository/jcenter" }' build.gradle
          else
              sed -i '/android {/i repositories {\n        maven { url "https://maven.aliyun.com/repository/jcenter" }\n        jcenter()\n        google()\n    }' build.gradle
          fi
          
          # ========== 验证修改结果 ==========
          echo "===== CordovaLib/build.gradle 关键部分 ====="
          grep -A 15 "buildscript {" build.gradle | head -20
          grep -A 10 "repositories {" build.gradle | head -15
          echo "============================================="

      # 8. 构建APK
      - name: Build Android APK
        run: |
          cd my-app
          cordova build android --debug --verbose

      # 9. 复制并上传APK
      - name: Copy & Upload APK
        run: |
          APK_PATH="my-app/platforms/android/app/build/outputs/apk/debug/app-debug.apk"
          if [ -f "$APK_PATH" ]; then
              cp "$APK_PATH" agora-audio-app-debug.apk
              ls -lh agora-audio-app-debug.apk
              echo "✅✅✅ APK生成成功！路径：$(pwd)/agora-audio-app-debug.apk"
              echo "应用名称: Agora Audio Call"
              echo "包名: com.example.agoraaudio"
              echo "版本: 1.0.0"
              echo "功能: 完整的声网音频通话应用"
          else
              echo "❌ APK未找到，打印目录："
              find my-app/platforms/android -name "*.apk" -type f | xargs ls -lh 2>/dev/null || true
              echo "检查构建输出："
              ls -la my-app/platforms/android/app/build/outputs/apk/ || true
              exit 1
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: agora-audio-apk
          path: agora-audio-app-debug.apk
          if-no-files-found: error