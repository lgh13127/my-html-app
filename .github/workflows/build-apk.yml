name: Build APK (assembleDebug) and upload artifact

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'   # <- 改为 17

      - name: Install prerequisites
        run: |
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends wget unzip

      - name: Set ANDROID_SDK_ROOT (use runner temp)
        run: |
          echo "ANDROID_SDK_ROOT=$RUNNER_TEMP/android-sdk" >> $GITHUB_ENV
          mkdir -p "$RUNNER_TEMP/android-sdk/cmdline-tools"

      - name: Install Android command line tools
        run: |
          cd "$RUNNER_TEMP/android-sdk"
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmdline-tools.zip
          unzip -q cmdline-tools.zip -d cmdline-tools
          mkdir -p cmdline-tools/latest
          mv cmdline-tools/cmdline-tools/* cmdline-tools/latest/ || true
          echo "PATH=$RUNNER_TEMP/android-sdk/cmdline-tools/latest/bin:$PATH" >> $GITHUB_ENV

      - name: Accept Android SDK licenses
        run: |
          yes | "$RUNNER_TEMP/android-sdk/cmdline-tools/latest/bin/sdkmanager" --licenses

      - name: Install Android SDK components
        run: |
          "$RUNNER_TEMP/android-sdk/cmdline-tools/latest/bin/sdkmanager" "platform-tools" "platforms;android-33" "build-tools;33.0.0"

      - name: Install Gradle 8.1.1 locally on runner
        run: |
          GRADLE_VERSION=8.1.1
          wget -q "https://services.gradle.org/distributions/gradle-${GRADLE_VERSION}-bin.zip" -O "$RUNNER_TEMP/gradle.zip"
          unzip -q "$RUNNER_TEMP/gradle.zip" -d "$RUNNER_TEMP"
          echo "GRADLE_HOME=$RUNNER_TEMP/gradle-${GRADLE_VERSION}" >> $GITHUB_ENV
          echo "PATH=$RUNNER_TEMP/gradle-${GRADLE_VERSION}/bin:$PATH" >> $GITHUB_ENV

      - name: Show Java and Gradle info
        run: |
          java -version || true
          gradle --version || true
          if [ -x "./gradlew" ]; then
            echo "Gradle wrapper exists; showing wrapper version:"
            ./gradlew --version || true
          fi

      - name: Build debug APK (capture log)
        run: |
          set -o pipefail
          mkdir -p build-logs
          if [ -x "./gradlew" ]; then
            ./gradlew clean assembleDebug --no-daemon --stacktrace 2>&1 | tee build-logs/build.log
            EXIT_CODE=${PIPESTATUS[0]}
          else
            gradle clean assembleDebug --no-daemon --stacktrace 2>&1 | tee build-logs/build.log
            EXIT_CODE=${PIPESTATUS[0]}
          fi
          echo "Gradle exit code: $EXIT_CODE"
          if [ "$EXIT_CODE" -ne 0 ]; then
            echo "::error::Gradle failed, see build-logs/build.log"
            exit $EXIT_CODE
          fi

      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Upload build log (for debugging)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gradle-build-log
          path: build-logs/build.log