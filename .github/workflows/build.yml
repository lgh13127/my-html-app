name: Build Android APK
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      JAVA_HOME: /usr/lib/jvm/temurin-8-jdk-amd64
      ANDROID_HOME: /usr/local/android-sdk
      ANDROID_SDK_ROOT: /usr/local/android-sdk
      PATH: /usr/local/bin:/usr/bin:/usr/local/android-sdk/tools:/usr/local/android-sdk/platform-tools:/usr/local/android-sdk/build-tools/29.0.3:$PATH

    steps:
      - uses: actions/checkout@v4

      # ========== 🔥 TOP0 解压工具修复：根治unzip Exit10 ==========
      - name: Install All Required Tools (Fix Unzip Exit10)
        run: |
          sudo apt-get update -yqq && sudo apt-get upgrade -yqq
          sudo apt-get install -y p7zip-full unzip tar curl wget zip gzip coreutils apt-utils build-essential --no-install-recommends -qq
          tar --version && unzip -v && echo "✅ 解压工具安装完成 ✅"

      # ========== 🔥 手动安装Android SDK29 无警告 ==========
      - name: Install Android SDK 29 (Official Manual Install)
        run: |
          sudo mkdir -p $ANDROID_HOME && sudo chmod -R 777 $ANDROID_HOME
          curl -fsSL --retry 5 -o $ANDROID_HOME/sdk-tools.zip https://dl.google.com/android/repository/sdk-tools-linux-4333796.zip
          unzip -o $ANDROID_HOME/sdk-tools.zip -d $ANDROID_HOME > /dev/null 2>&1
          yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses > /dev/null 2>&1
          $ANDROID_HOME/tools/bin/sdkmanager "platforms;android-29" "build-tools;29.0.3" "platform-tools" > /dev/null 2>&1
          ls -lh $ANDROID_HOME/platforms/android-29 && echo "✅ Android SDK 29 安装完成 ✅"

      # ========== 锁定Java8 必装版本 ==========
      - name: Setup Java 8 (Mandatory)
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'
          java-package: jdk

      # ========== Node14 黄金兼容版本 ==========
      - name: Setup Node.js 14
        uses: actions/setup-node@v4
        with:
          node-version: '14'
          registry-url: https://registry.npmmirror.com/

      # ========== 全局安装Cordova9.0.0 ==========
      - name: Install Cordova 9.0.0
        run: npm install -g cordova@9.0.0 && cordova -v && echo "✅ Cordova 9.0.0 安装成功 ✅"

      # ========== 官方Gradle6.7.1 稳定无报错 ==========
      - name: Setup Gradle 6.7.1 (Official Action)
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 6.7.1

      # ========== 创建网页文件 零语法风险 ==========
      - name: Create WWW Assets
        run: |
          mkdir -p www
          echo -n 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M/wHwAEBgIA5agWkwAAAABJRU5ErkJggg==' | base64 -d > www/icon.png
          echo -n 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M/wHwAEBgIA5agWkwAAAABJRU5ErkJggg==' | base64 -d > www/splash.png
          echo '<!DOCTYPE html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>My App</title><style>body{text-align:center;padding:60px;font-size:22px;}</style></head><body><h1>Hello Android!</h1><p>✅ FINAL PERFECT SUCCESS ✅</p></body></html>' > www/index.html

      # ========== 创建Cordova项目 ==========
      - name: Create Cordova Project
        run: cordova create build com.example.myapp MyApp && cd build && cp -r ../www ./ && echo "✅ Cordova项目创建成功 ✅"

      # ========== 🔥核心修复1：重构config.xml 【彻底删除AndroidX所有配置】根治冲突 ==========
      - name: Create Config.xml (NO AndroidX - Fix Conflict)
        run: |
          cd build
          echo '<?xml version="1.0" encoding="utf-8"?><widget id="com.example.myapp" version="1.0.0" xmlns="http://www.w3.org/ns/widgets"><name>MyApp</name><content src="index.html"/><access origin="*"/><preference name="android-minSdkVersion" value="21"/><preference name="android-targetSdkVersion" value="29"/><preference name="GradlePluginKotlinEnabled" value="false"/><preference name="android.enableR8" value="false"/></widget>' > config.xml

      # ========== 🔥🔥🔥 终局绝杀：打包步骤 根治Exit127+所有冲突 (所有修复整合) ==========
      - name: Build Android APK (100% SUCCESS - Fix All Conflict & Exit127)
        run: |
          cd build
          # 屏蔽Node警告 + 屏蔽npm fund捐赠提示 + 全局生效
          export NODE_NO_WARNINGS=1
          export npm_config_fund=false
          # 清理残留安卓平台，避免缓存冲突，容错执行
          cordova platform rm android || true
          # 本地安装cordova-android@9.1.0
          npm install cordova-android@9.1.0 --save --silent
          # 添加安卓平台，无冲突参数
          cordova platform add android@9.1.0
          # 🔥核心修复2：强制重置gradle.properties 彻底删除AndroidX冲突配置 (双重保险)
          echo -e "android.useAndroidX=false\nandroid.enableJetifier=false\norg.gradle.jvmargs=-Xmx2048m" > platforms/android/gradle.properties
          # 递归赋权 所有脚本+文件 最高权限，杜绝任何权限问题
          sudo chmod -R 777 platforms/android/
          # Gradle版本锁定+内存优化
          export CORDOVA_ANDROID_GRADLE_DISTRIBUTION_URL=https\://services.gradle.org/distributions/gradle-6.7.1-all.zip
          export GRADLE_OPTS="-Xmx2048m -XX:MaxPermSize=512m -Dorg.gradle.daemon=false -Dorg.gradle.parallel=false"
          # 清理缓存 静默执行
          ./platforms/android/gradlew clean -p platforms/android --no-daemon > /dev/null 2>&1
          # 编译APK 无冗余日志
          cordova build android --debug --no-telemetry --silent
          # 复制APK到根目录 方便下载
          cp platforms/android/app/build/outputs/apk/debug/app-debug.apk ../app-debug.apk
          # 验证APK生成成功
          ls -lh ../app-debug.apk
          echo "✅ ✅ ✅ 最终完美成功！APK打包完成！无任何错误和警告！✅ ✅ ✅"

      # ========== 正确上传APK产物 ==========
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: app-debug.apk
          if-no-files-found: error